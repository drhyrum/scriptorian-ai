services:
  - type: web
    name: scriptorian
    env: python
    buildCommand: "pip install -e '.[server]'"
    startCommand: "python -m scriptorian.server_sse"
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
