services:
  - type: web
    name: scriptorian
    env: python
    buildCommand: "pip install -e .[server]"
    startCommand: "uvicorn scriptorian.server_sse:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
